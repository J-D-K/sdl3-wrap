#pragma once

/// @brief This is just dimensions used by all of the menu pieces.
namespace ui::menudims
{
    /// @brief The padding used for width.
    inline constexpr int PADDING = 24;

    /// @brief Margin used in between menus.
    inline constexpr int MARGIN = 1;

    /// @brief The height.
    inline constexpr int HEIGHT = 20;

    /// @brief X offset used when rendering labels of sub-options.
    inline constexpr int SUB_OFFSET_X = 8;
}